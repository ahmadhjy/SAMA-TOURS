"""
Paste into PythonAnywhere Web tab → WSGI configuration file.
Account: Samatours2026 | Website only (ERP is separate at Sama-Acc)
"""

import os
import sys

PROJECT_HOME = "/home/Samatours2026/SAMA-TOURS"
if PROJECT_HOME not in sys.path:
    sys.path.insert(0, PROJECT_HOME)

os.environ["DJANGO_SETTINGS_MODULE"] = "config.settings.production"

os.environ["DJANGO_SECRET_KEY"] = "REPLACE_WITH_LONG_RANDOM_SECRET_KEY"

# Comma-separated — ONE string (not a Python tuple with commas inside parentheses)
os.environ["DJANGO_ALLOWED_HOSTS"] = (
    "main-samatours2026.pythonanywhere.com,"
    "www.samatourslb.com,"
    "samatourslb.com"
)
os.environ["DJANGO_CSRF_TRUSTED_ORIGINS"] = (
    "https://main-samatours2026.pythonanywhere.com,"
    "https://www.samatourslb.com,"
    "https://samatourslb.com"
)
os.environ["DJANGO_DEBUG"] = "False"

os.environ["DJANGO_DB_ENGINE"] = "postgresql"
os.environ["DJANGO_DB_NAME"] = "sama_website"
os.environ["DJANGO_DB_USER"] = "sama_web"
os.environ["DJANGO_DB_PASSWORD"] = "REPLACE_WEBSITE_DB_PASSWORD"
os.environ["DJANGO_DB_HOST"] = "Samatours2026-5298.postgres.pythonanywhere-services.com"
os.environ["DJANGO_DB_PORT"] = "15298"

from django.core.wsgi import get_wsgi_application

application = get_wsgi_application()
